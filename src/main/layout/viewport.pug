template
    div(show="false" if.bind="anchor").viewport-anchor
    slot(name="header")
    .main(ref="main")
        slot(name="navigation")
        main
            slot(name="left")
            slot
            slot(name="right")
    slot(name="footer")
